{
  "adapters": [
    {
      "name": "gpt-4o",
      "modelPattern": "gpt-4o",
      "temperature": 0.1,
      "maxOutputTokensBatch": 16000,
      "maxOutputTokensSingleFile": 4000,
      "maxOutputTokensVerification": 2000,
      "maxOutputTokensPrSummary": 4000,
      "maxInputLinesPerFile": 5000,
      "promptStyle": "imperative",
      "preamble": "CRITICAL: You MUST respond ONLY with valid, parseable JSON — no markdown fences, no commentary outside the JSON object. If your response cannot be parsed as JSON, the entire review is discarded.",
      "quirks": [
        "May produce trailing text after JSON when temperature > 0.2",
        "Occasionally wraps JSON in markdown ```json fences despite instruction not to",
        "Temperature > 0.3 increases hallucination risk for line numbers"
      ]
    },
    {
      "name": "gpt-4",
      "modelPattern": "^gpt-4(?!o|\\d{4})",
      "temperature": 0.05,
      "maxOutputTokensBatch": 7000,
      "maxOutputTokensSingleFile": 2000,
      "maxOutputTokensVerification": 1500,
      "maxOutputTokensPrSummary": 2000,
      "maxInputLinesPerFile": 3000,
      "promptStyle": "imperative",
      "preamble": "CRITICAL: You MUST respond ONLY with valid, parseable JSON — no markdown fences, no commentary outside the JSON object. Keep responses concise due to output token constraints. Prefer fewer, higher-quality comments over exhaustive coverage.",
      "quirks": [
        "8K max output tokens — keep reviews compact",
        "More conservative with line number accuracy than gpt-4o",
        "Better at following strict format rules but slower",
        "Temperature sensitivity: even 0.2 can cause format drift"
      ]
    },
    {
      "name": "default",
      "modelPattern": ".*",
      "promptStyle": "imperative",
      "preamble": "CRITICAL: You MUST respond ONLY with valid, parseable JSON — no markdown fences, no commentary outside the JSON object.",
      "quirks": [
        "Generic adapter — no model-specific tuning applied",
        "All numeric parameters inherit from ReviewProfile defaults"
      ]
    }
  ]
}
